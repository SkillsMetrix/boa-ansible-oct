# pre task called before main task
- name: check the background of remote machine
  apt:
    update_cache: true
    cache_valid_time: 3600

- name: Gather OS Info
  setup:
    gather_subset:
      - "os_family"

- name: print OS Info
  debug:
    msg: "the remote OS ver is {{ansible_os_family}}"